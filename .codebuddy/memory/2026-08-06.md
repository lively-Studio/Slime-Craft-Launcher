# 2026-08-06 工作记录

## SCL 构建成功

### 完成的改名工作
- `hmcl://` 内部协议 → `scl://`（代码+ I18N 全局替换）
- 所有硬编码 "HMCL"/"hmcl" 字符串 → "SCL"/"scl"（系统属性键、env var、路径、UserAgent等）
- 类名中 HMCL → SCL（sed 全局替换因 macOS \b 不兼容，改用无边界替换）
- 构建文件中的模块引用、资源路径全部修复
- 启动器图标替换为新的 SCL 六边形+草方块图标 (300x300 PNG)

### 构建命令
```bash
cd /Users/cangcang/Downloads/HMCL-main
export JAVA_HOME=/Users/cangcang/Documents/jdk21/Contents/Home
export _JAVA_OPTIONS="-Djdk.lang.Process.launchMechanism=FORK"
./gradlew :SCL:jar :SCLCore:jar :SCLBoot:jar --no-daemon -Dorg.gradle.java.home=/Users/cangcang/Documents/jdk21/Contents/Home -x test -x compileTestJava
```

### 构建输出
- SCL/build/libs/SCL-3.17.SNAPSHOT.jar (9.6MB shadowJar)
- SCLCore/build/libs/SCLCore.jar (1.5MB)
- SCLBoot/build/libs/SCLBoot.jar (12KB)

### 注意事项
- 需要 Microsoft JDK 21.0.7 (`~/Documents/jdk21/Contents/Home`)
- 必须设置 `_JAVA_OPTIONS=-Djdk.lang.Process.launchMechanism=FORK`(posix_spawn 兼容)
- 测试目录 `src/test/java/org/jackhuang/hmcl/` 未移动（含旧引用，需-x test -x compileTestJava跳过）
- ~/.gradle/gradle.properties 中的旧JDK路径需保留或更新
